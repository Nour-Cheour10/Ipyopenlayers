{
  "scripts": {
    "dev": "npm run build -- --sourcemap=inline --watch",
    "build": "esbuild --minify --format=esm --bundle --outdir=src/xarray_fancy_repr/static js/widget.js",
    "typecheck": "tsc --noEmit",
    "format": "prettier --ignore-path=.gitignore --write .",
    "lint": "prettier --ignore-path=.gitignore --check ."
  },
  "dependencies": {
    "@anywidget/react": "^0.0.2",
    "i": "^0.3.7",
    "ol": "^9.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xarray": "^0.0.3"
  },
  "devDependencies": {
    "@types/react": "^18.2.21",
    "@types/react-dom": "^18.2.7",
    "esbuild": "^0.19.2",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
