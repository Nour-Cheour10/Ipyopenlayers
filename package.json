{
  "name": "anywidget_n",
  "lockfileVersion": 3,
  "requires": true,
  "packages": {},
  "dependencies": {
    "cartodb": "^0.5.1",
    "ol": "9.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "prettier": "^3.0.3",
    "vite": "^3.2.3"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "esobuild": "esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/widget.js",
    "ess-build": "esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/Cartodb.js",
    "ebuild": "esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/CartodbLayer.js",
    "elbuild": "esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/layer.js",

    "esbuild": "esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/widget.js && esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/Cartodb.js && esbuild --minify --format=esm --bundle --outdir=src/IpyOpenLayer_repr/static js/layer.js"
  }
}
