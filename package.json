{
  "name": "anywidget_n",
  "lockfileVersion": 3,
  "requires": true,
  "packages": {},
  "dependencies": {
    "ol": "9.1.0"
  },
  "devDependencies": {
    "vite": "^3.2.3",
    "esbuild": "^0.19.2",
    "prettier": "^3.0.3"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "esbuild": "esbuild --minify --format=esm --bundle --outdir=src/xarray_fancy_repr/static js/widget.js"
    
  }
}
